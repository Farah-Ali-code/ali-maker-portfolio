/* ============================================================
   style.css — Maker Portfolio
   Structure :
     1. Variables & Reset
     2. Scrollbar
     3. Navigation
     4. Hero
     5. Shared section utilities
     6. Gallery (scroll horizontal piloté par scroll vertical)
     7. Projects
     8. Process (timeline)
     9. Contact
    10. Footer
    11. Animations & utilitaires
   ============================================================ */


/* ─────────────────────────────────────────────
   1. VARIABLES & RESET
   Toutes les couleurs du thème dark-editor sont
   définies ici comme custom properties CSS.
   ───────────────────────────────────────────── */
:root {
  /* Fonds — du plus sombre au plus clair */
  --void:    #050112;   /* fond absolu, utilisé comme base page */
  --deep:    #0a0525;   /* sections alternées légèrement plus claires */
  --surface: #110a30;   /* cartes, panneaux */
  --rim:     #2d1f6e;   /* bordures neutres */
  --dim:     #3e3470;   /* éléments décoratifs discrets */

  /* Couleurs néon — palette principale */
  --neon-violet: #b44bff;
  --neon-blue:   #3d9eff;
  --neon-cyan:   #00e5ff;
  --neon-green:  #39ff9a;
  --muted:       #6b5fa0;  /* violet atténué pour les textes secondaires */

  /* Texte */
  --text-main:  #e8e0ff;  /* texte principal */
  --text-sub:   #cfb6b6;  /* texte secondaire / descriptions */
  --text-ghost: #8e86a8;  /* texte très discret, labels, meta */

  /* Typographie */
  --mono:    'JetBrains Mono', monospace;  /* corps, nav, labels */
  --display: 'Syne', sans-serif;           /* titres */
}

/* Reset minimal — normalise les marges et le box model */
*, *::before, *::after {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html {
  scroll-behavior: smooth;
}

body {
  background: var(--void);
  color: var(--text-main);
  font-family: var(--mono);
  overflow-x: hidden;  /* empêche le scroll horizontal parasite */
}


/* ─────────────────────────────────────────────
   2. SCROLLBAR PERSONNALISÉE
   Style webkit uniquement (Chrome, Edge, Safari)
   ───────────────────────────────────────────── */
::-webkit-scrollbar        { width: 6px; }
::-webkit-scrollbar-track  { background: var(--void); }
::-webkit-scrollbar-thumb  { background: var(--rim); border-radius: 3px; }
::-webkit-scrollbar-thumb:hover { background: var(--neon-violet); }


/* ─────────────────────────────────────────────
   3. NAVIGATION
   Fixée en haut. Transparente sur le hero,
   elle devient opaque + blur dès qu'on scrolle
   (la classe .scrolled est ajoutée par JS).
   ───────────────────────────────────────────── */
nav {
  position: fixed;
  top: 0; left: 0; right: 0;
  z-index: 200;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0 3rem;
  height: 60px;

  /* Valeurs initiales — transparent sur le hero */
  background: rgba(5, 1, 18, 0);
  backdrop-filter: blur(0);
  border-bottom: 1px solid rgba(100, 60, 200, 0);

  transition: background .4s, backdrop-filter .4s, border-color .4s;
}

/* État après scroll — ajouté par JS */
nav.scrolled {
  background: rgba(5, 1, 18, .88);
  backdrop-filter: blur(18px);
  border-bottom-color: rgba(100, 60, 200, .18);
}

/* Logo / brand */
.nav-brand {
  font-family: var(--mono);
  font-size: .85rem;
  color: var(--neon-cyan);
  letter-spacing: .05em;
}
.nav-brand span { color: var(--muted); } /* le ~/ avant le nom */

/* Liste des liens */
.nav-links {
  display: flex;
  align-items: center;
  list-style: none;
}

/* Séparateur // entre chaque lien */
.nav-sep {
  color: var(--dim);
  font-size: .8rem;
  padding: 0 6px;
  user-select: none;
}

/* Liens individuels */
.nav-links a {
  color: var(--text-sub);
  text-decoration: none;
  font-size: .72rem;
  letter-spacing: .12em;
  text-transform: uppercase;
  padding: 4px 2px;
  transition: color .2s;
  position: relative;
}

/* Soulignement animé au hover */
.nav-links a::after {
  content: '';
  position: absolute;
  bottom: -2px; left: 0; right: 0;
  height: 1px;
  background: var(--neon-violet);
  transform: scaleX(0);
  transition: transform .2s;
}
.nav-links a:hover { color: var(--neon-violet); }
.nav-links a:hover::after { transform: scaleX(1); }

/* Indicateur de statut (point vert + texte) */
.nav-status {
  display: flex;
  align-items: center;
  gap: 7px;
  font-size: .7rem;
  color: var(--text-ghost);
}

.status-dot {
  width: 7px; height: 7px;
  border-radius: 50%;
  background: var(--neon-green);
  box-shadow: 0 0 8px var(--neon-green);
  animation: pulse 2.4s ease-in-out infinite;
}


/* ─────────────────────────────────────────────
   4. HERO
   Plein écran. L'image de fond est injectée
   via JS pour gérer le fade-in au chargement.
   ───────────────────────────────────────────── */
#hero {
  position: relative;
  height: 100vh;
  display: flex;
  flex-direction: column;
  justify-content: flex-end;  /* contenu collé en bas */
  overflow: hidden;
}

/* Div qui porte le background-image (injecté par JS) */
.hero-img {
  position: absolute; inset: 0;
  background-size: cover;
  background-position: center 30%;
  /* léger dézoom initial pour l'effet ken burns */
  transform: scale(1.06);
  transition: transform 8s ease-out;
}
/* Quand l'image est chargée, JS ajoute .loaded → on lance le dézoom */
.hero-img.loaded { transform: scale(1); }

/* Dégradé sombre du bas vers le haut pour lisibilité du texte */
.hero-overlay {
  position: absolute; inset: 0;
  background: linear-gradient(
    to top,
    rgba(5, 1, 18, 1.0)  0%,
    rgba(5, 1, 18, .75) 30%,
    rgba(5, 1, 18, .2)  65%,
    rgba(5, 1, 18, .05) 100%
  );
}

/* Tinte violet en bas à gauche pour l'ambiance */
.hero-tint {
  position: absolute; inset: 0;
  background: radial-gradient(ellipse 70% 60% at 20% 80%, rgba(80, 20, 180, .35), transparent 70%);
}

/* Contenu texte — positionné au-dessus des overlays */
.hero-content {
  position: relative;
  z-index: 2;
  padding: 0 3rem 5rem;
  max-width: 820px;
}

/* Petite ligne de contexte au-dessus du H1 */
.hero-eyebrow {
  font-size: .72rem;
  color: var(--neon-cyan);
  letter-spacing: .28em;
  text-transform: uppercase;
  margin-bottom: 1.2rem;
  display: flex;
  align-items: center;
  gap: 10px;
  /* animation d'entrée — définie dans section 11 */
  opacity: 0;
  transform: translateY(14px);
  animation: fadeUp .7s .2s forwards;
}
.hero-eyebrow::before {
  content: '';
  display: inline-block;
  width: 28px; height: 1px;
  background: var(--neon-cyan);
  box-shadow: 0 0 8px var(--neon-cyan);
}

/* Titre principal — texte en dégradé */
h1 {
  font-family: var(--display);
  font-size: clamp(3.2rem, 8vw, 7rem);
  font-weight: 800;
  line-height: .95;
  letter-spacing: -.03em;
  background: linear-gradient(135deg, #fff 0%, var(--neon-violet) 50%, var(--neon-blue) 85%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  margin-bottom: 1.6rem;
  opacity: 0;
  transform: translateY(20px);
  animation: fadeUp .8s .4s forwards;
}

/* Boutons CTA */
.hero-cta {
  display: flex;
  gap: 1rem;
  align-items: center;
  opacity: 0;
  transform: translateY(14px);
  animation: fadeUp .7s .7s forwards;
}

/* Bouton primaire — fond dégradé + coin coupé */
.btn-primary {
  background: linear-gradient(135deg, var(--neon-violet), var(--neon-blue));
  color: #fff;
  border: none;
  padding: .75rem 2.2rem;
  font-family: var(--mono);
  font-size: .8rem;
  letter-spacing: .1em;
  cursor: pointer;
  text-decoration: none;
  display: inline-block;
  /* forme polygonale asymétrique (coin bas-gauche et haut-droite coupés) */
  clip-path: polygon(0 0, calc(100% - 10px) 0, 100% 10px, 100% 100%, 10px 100%, 0 calc(100% - 10px));
  transition: filter .2s;
}
.btn-primary:hover { filter: brightness(1.25); }

/* Bouton secondaire — juste un soulignement */
.btn-ghost {
  color: var(--text-sub);
  text-decoration: none;
  font-size: .78rem;
  letter-spacing: .08em;
  border-bottom: 1px solid var(--dim);
  padding-bottom: 2px;
  transition: color .2s, border-color .2s;
}
.btn-ghost:hover { color: var(--neon-cyan); border-color: var(--neon-cyan); }





/* ─────────────────────────────────────────────
   5. UTILITAIRES PARTAGÉS ENTRE SECTIONS
   ───────────────────────────────────────────── */

/* Label de section (// gallery, // projects…) */
.sec-label {
  font-size: .68rem;
  letter-spacing: .3em;
  text-transform: uppercase;
  color: var(--neon-blue);
  margin-bottom: .6rem;
  display: flex;
  align-items: center;
  gap: 10px;
}
.sec-label::before { content: '//'; color: var(--dim); }

/* Titre de section */
.sec-title {
  font-family: var(--display);
  font-size: clamp(1.8rem, 3.5vw, 2.8rem);
  font-weight: 700;
  color: var(--text-main);
  letter-spacing: -.01em;
}


/* ─────────────────────────────────────────────
   6. GALLERY — SCROLL HORIZONTAL
   Principe : le container fait 400vh de haut.
   L'intérieur est en position sticky → il reste
   à l'écran pendant qu'on scrolle. JS lit la
   progression du scroll et déplace la track.
   ───────────────────────────────────────────── */
#gallery {
  background: var(--void);
  padding: 6rem 0;
}

.gallery-header {
  padding: 0 3rem;
  margin-bottom: 3rem;
}

.gallery-scroll-container {
  display: flex;
  flex-wrap: nowrap;   /* Force les images à rester sur une seule ligne */
  width: max-content;  /* Magique : le conteneur prend la largeur exacte de toutes les images combinées */
}

.galery-element {
  position: relative;
  /* 25vw permet d'en voir 4 entières, donc pour en cropper, 
     on peut rester sur 30vw ou 40vw selon l'effet voulu */
  width: 40vw; 
  flex-shrink: 0; 
  height: 50vh;          /* Définit une hauteur fixe ou auto selon tes images */
  margin: 0 10px;        /* Petit espace entre les photos */
  overflow: hidden;      /* Pour que l'image ne dépasse pas du cadre arrondi par ex */
}

.galery-element img {
  width: 100%;
  height: 100%;          /* Remis à 100% pour remplir le cadre */
  object-fit: cover;
  display: block;
}

/* 1. L'enveloppe qui définit la zone de travail */
.gallery-wrapper {
  position: relative; /* C'est le nouveau référentiel pour les boutons */
  width: 100%;
  overflow: hidden;   /* On coupe le ruban d'images qui dépasse ici */
}

/* 2. Les contrôleurs (fixes sur les côtés) */
.gallery-scroll.controller {
  position: absolute;
  top: 0;
  height: 100%;
  width: 20vw;
  z-index: 10;        /* Passe par-dessus les images */
  pointer-events: all; /* S'assure qu'on peut cliquer dessus */
  cursor: pointer;
}

.gallery-scroll.controller.left {
  left: 0;
  background: linear-gradient( to left, transparent,hsl(from var(--dim) h s l ));
  z-index: 50;
  opacity: 0.5;
}

.gallery-scroll.controller.right {
  right: 0;           /* On utilise right: 0 plutôt que left: 75% */
    background: linear-gradient( to right, transparent,hsl(from var(--dim) h s l ));
  z-index: 50;
}



/* Caption en bas de chaque panel — masquée par défaut */
.panel-caption {
  width: 100%;
  height: 20%;
  position: absolute;
  bottom: 0; left: 0; right: 0;
  background: linear-gradient(to top, rgba(5, 1, 18, .92) 0%, transparent 100%);
  z-index: 2;
  transform: translateY(12px);

  transition: opacity .5s, transform .5s;
}


/* Numéro de panel avec couleur par thème */
.panel-num {
  font-size: .65rem;
  letter-spacing: .25em;
  text-transform: uppercase;
  margin-bottom: .5rem;
}
.panel-num.c1 { color: var(--neon-cyan);   }
.panel-num.c2 { color: var(--neon-violet); }
.panel-num.c3 { color: var(--neon-green);  }
.panel-num.c4 { color: #ffb340;            }

.panel-title {
  font-family: var(--display);
  font-size: clamp(0.2rem, 2vw, 1.2rem);
  font-weight: 800;
  color: #fff;
  letter-spacing: -.02em;
  margin-bottom: .5rem;
}
.panel-sub {
  font-size: .8rem;
  color: var(--text-sub);
  max-width: 480px;
  line-height: 1.8;
}


/* ─────────────────────────────────────────────
   7. PROJECTS
   Chaque projet a ses propres tokens de couleur
   via les classes .p-cyan / .p-violet / .p-amber.
   ───────────────────────────────────────────── */
   #projects{
    display: flex;
    flex-direction: column;
    align-items: center;
    padding-bottom: 10%;
    position: relative;
   }
 #projects_bg {
  position: absolute;
  top: 0;
  left: 0;
  
  padding: 0px;
  
  width: 100%;
  height: 100%;
  padding: 6rem 3rem;
  z-index: -2;
  background-repeat: no-repeat;
  filter: blur(4px);
}

.project-row > .blur{
    position: absolute;
    z-index: -1;
  top: 0;    /* <-- INDISPENSABLE */
  left: 0;
  width: 100%;
  height: 100%;
  backdrop-filter: blur(10px);
}

/* Tokens couleur par projet
   --pc        : couleur principale
   --pc-dim    : version très transparente (fond tags, badges)
   --pc-glow   : version pour les halos
   --pc-border : version pour les bordures
   Ces tokens sont utilisés partout dans les children. */
.p-cyan   { --pc: #00e5ff; --pc-dim: rgba(0,   229, 255, .10); --pc-glow: rgba(0,   229, 255, .22); --pc-border: rgba(0,   229, 255, .32); }
.p-violet { --pc: #b44bff; --pc-dim: rgba(180,  75, 255, .10); --pc-glow: rgba(180,  75, 255, .22); --pc-border: rgba(180,  75, 255, .32); }
.p-amber  { --pc: #ffb340; --pc-dim: rgba(255, 179,  64, .08); --pc-glow: rgba(255, 179,  64, .20); --pc-border: rgba(255, 179,  64, .32); }

/* Wrapper vertical */
.cascade-wrapper {
  
  display: flex;
  flex-direction: column;
  gap: 3.5rem;
  max-width: 1100px;
}

/* Carte projet : grille 2 colonnes (photo | contenu) */
.project-row {
  position: relative;
  display: grid;
  grid-template-columns: 1fr 1fr;
  /*background: var(--surface);*/
background: #110A30;
background: linear-gradient(0deg, hsl(from var(--pc) h s l /0.2) 0%,  hsl(from var(--pc) h s l /0.2) 100%);

  overflow: hidden;
  position: relative;
  transition: border-color .3s, transform .3s;
}


/* Trait de couleur en haut de la carte */
.project-row::before {
  content: '';
  position: absolute;
  top: 0; left: 0; right: 0;
  height: 2px;
  background: linear-gradient(90deg, var(--pc, var(--neon-violet)), transparent);
  z-index: 2;
}




/* Classe .flip pour alterner photo gauche/droite */
.project-row.flip       { direction: rtl; }
.project-row.flip > *   { direction: ltr; }

/* Photo du projet */
.proj-photo {
  position: relative;
  overflow: hidden;
  aspect-ratio: 4 / 3;
  background: #08041f;
}
.proj-photo img {
  width: 100%; height: 100%;
  object-fit: cover;
  filter: saturate(.7) brightness(.82);
  transition: filter .4s, transform .5s;
}
.project-row:hover .proj-photo img {
  backdrop-filter: blur(20px);
}
/* Overlay coloré sur la photo */
.proj-photo::after {
  content: '';
  position: absolute; inset: 0;
  background: linear-gradient(135deg, var(--pc-dim, rgba(81, 255, 75, 0.12)) 0%, transparent 60%);
  mix-blend-mode: screen;
  pointer-events: none;
}
/* Numéro et catégorie sur la photo */
.proj-photo-num {
  position: absolute;
  bottom: 12px; left: 14px;
  font-size: .65rem;
  letter-spacing: .2em;
  color: var(--pc, var(--neon-violet));
  z-index: 2;
  text-shadow: 0 0 10px var(--pc-glow, rgba(180,75,255,.5));
}

/* Zone texte de la carte */
.proj-content {
  position: relative;
  padding: 2.2rem 2rem;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  position: relative;
  z-index: 1;
}


.proj-header {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  margin-bottom: 1rem;
  gap: 1rem;
}
.proj-name {
  font-family: var(--display);
  font-size: 1.4rem;
  font-weight: 700;
  color: var(--text-main);
  letter-spacing: -.01em;
}
.proj-type {
  font-size: .62rem;
  letter-spacing: .15em;
  text-transform: uppercase;
  padding: 3px 10px;
  border: 1px solid var(--pc, var(--neon-violet));
  color: var(--pc, var(--neon-violet));
  flex-shrink: 0;
  background: var(--pc-dim, transparent);
}

.proj-desc {
  font-size: .83rem;
  line-height: 1.9;
  color: var(--text-sub);
  margin-bottom: 1.6rem;
  flex: 1;
}

/* Tableau de specs techniques */
.proj-specs {
  display: flex;
  flex-wrap: wrap;
  gap: 8px 16px;
  margin-bottom: 1.4rem;
  padding: 1rem;
  background: rgba(5, 1, 18, .5);
  border-left: 2px solid var(--pc, var(--neon-violet));
}
.spec-item {
  font-size: .7rem;
  color: var(--text-ghost);
}
.spec-item strong {
  color: var(--pc, var(--neon-violet));
  font-weight: 500;
  display: block;
  font-size: .64rem;
  letter-spacing: .1em;
  text-transform: uppercase;
  margin-bottom: 1px;
}

/* Tags technos en bas de carte */
.proj-tags { display: flex; gap: 6px; flex-wrap: wrap; }
.tag {
  font-size: .65rem;
  padding: 3px 10px;
  background: var(--pc-dim, rgba(45,31,110,.6));
  border: 1px solid var(--pc-border, var(--dim));
  color: var(--pc, var(--muted));
  letter-spacing: .06em;
}


/* ─────────────────────────────────────────────
   8. PROCESS (TIMELINE)
   Ligne verticale avec des points lumineux
   et un contenu texte à droite.
   ───────────────────────────────────────────── */
#process {
  padding: 6rem 3rem;
}

.timeline {
  display: flex;
  flex-direction: column;
  max-width: 760px;
  position: relative;
}
/* Ligne verticale continue derrière les points */
.timeline::before {
  content: '';
  position: absolute;
  top: 0; left: 14px; bottom: 0;
  width: 1px;
  background: linear-gradient(to bottom, var(--neon-blue), var(--neon-violet), transparent);
}

/* Item = grille 2 col (point | texte) */
.tl-item {
  display: grid;
  grid-template-columns: 30px 1fr;
  gap: 0 2rem;
  padding-bottom: 2.8rem;
}

/* Points colorés — une couleur par étape */
.tl-dot {
  width: 14px; height: 14px;
  border-radius: 50%;
  margin-top: 3px;
  position: relative;
  z-index: 1;  /* passe au-dessus de la ligne ::before */
}
.tl-dot.v { background: var(--neon-violet); box-shadow: 0 0 14px var(--neon-violet); }
.tl-dot.b { background: var(--neon-blue);   box-shadow: 0 0 14px var(--neon-blue);   }
.tl-dot.c { background: var(--neon-cyan);   box-shadow: 0 0 14px var(--neon-cyan);   }
.tl-dot.g { background: var(--neon-green);  box-shadow: 0 0 14px var(--neon-green);  }

.tl-phase {
  font-size: .68rem;
  letter-spacing: .2em;
  text-transform: uppercase;
  color: var(--muted);
  margin-bottom: 4px;
}
.tl-title {
  font-family: var(--display);
  font-size: 1.1rem;
  font-weight: 600;
  color: var(--text-main);
  margin-bottom: .5rem;
}
.tl-desc {
  font-size: .8rem;
  line-height: 1.85;
  color: var(--text-sub);
}


/* ─────────────────────────────────────────────
   9. CONTACT
   ───────────────────────────────────────────── */
#contact {
  background: var(--void);
  text-align: center;
  padding: 6rem 3rem 0;
}

.contact-inner {
  max-width: 600px;
  margin: 0 auto;
  padding-bottom: 6rem;
}

.contact-address {
  font-size: .85rem;
  letter-spacing: .05em;
  color: var(--text-sub);
  margin: 2rem 0;
  line-height: 2;
}
.contact-address a {
  color: var(--neon-violet);
  text-decoration: none;
  transition: text-shadow .2s;
}
.contact-address a:hover { text-shadow: 0 0 18px var(--neon-violet); }

.social-row {
  display: flex;
  justify-content: center;
  gap: 2rem;
  margin-top: 2rem;
}
.social-row a {
  font-size: .75rem;
  letter-spacing: .1em;
  text-transform: uppercase;
  color: var(--text-ghost);
  text-decoration: none;
  border-bottom: 1px solid var(--dim);
  padding-bottom: 2px;
  transition: all .2s;
}
.social-row a:hover { color: var(--neon-cyan); border-color: var(--neon-cyan); }


/* ─────────────────────────────────────────────
   10. FOOTER
   La courbe concave en haut est un SVG inline
   dans le HTML, positionné en absolu juste
   au-dessus du footer.
   ───────────────────────────────────────────── */
footer {
  position: relative;
  background: var(--surface);
  padding: 3rem 3rem 2rem;
}

/* Conteneur du SVG de la courbe concave */
.footer-concave {
  position: absolute;
  top: -80px; left: 0; right: 0;
  height: 80px;
  overflow: hidden;
}
.footer-concave svg { width: 100%; height: 100%; display: block; }

.footer-inner {
  display: flex;
  align-items: center;
  justify-content: space-between;
  flex-wrap: wrap;
  gap: 1rem;
}

.footer-brand { font-size: .78rem; color: var(--text-ghost); letter-spacing: .06em; }
.footer-brand span { color: var(--neon-violet); }

.footer-copy { font-size: .68rem; color: var(--text-ghost); letter-spacing: .06em; }

.footer-links { display: flex; gap: 1.5rem; list-style: none; }
.footer-links a {
  font-size: .7rem;
  color: var(--text-ghost);
  text-decoration: none;
  letter-spacing: .08em;
  transition: color .2s;
}
.footer-links a:hover { color: var(--neon-blue); }


/* ─────────────────────────────────────────────
   11. ANIMATIONS & UTILITAIRES
   ───────────────────────────────────────────── */

/* Clignotement du point de statut nav */
@keyframes pulse {
  0%, 100% { opacity: 1;  }
  50%       { opacity: .3; }
}

/* Entrée de bas en haut (hero eyebrow, h1, CTA…) */
@keyframes fadeUp {
  to { opacity: 1; transform: none; }
}

/* Ligne d'invitation à scroller — remplit de haut en bas */
@keyframes scrollAnim {
  0%   { transform: scaleY(0); transform-origin: top;    }
  50%  { transform: scaleY(1); transform-origin: top;    }
  51%  { transform-origin: bottom;                       }
  100% { transform: scaleY(0); transform-origin: bottom; }
}

/* Classe utilitaire pour les apparitions au scroll
   JS ajoute .visible via IntersectionObserver */
.reveal {
  opacity: 0;
  transform: translateY(28px);
  transition: opacity .6s ease, transform .6s ease;
}
.reveal.visible {
  opacity: 1;
  transform: none;
}














#glitch-canvas {
  position: fixed;
  top: 0; left: 0;
  width: 100vw; height: 100vh;
  z-index: -2;
  background-color: #050112; /* Le fond très sombre */
  background-repeat: no-repeat;
}